# pyproject.toml
[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = ["src/db/migrations/versions"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
